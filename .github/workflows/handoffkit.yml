name: HandoffKit

on:
  pull_request:

jobs:
  handoffkit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          path: .
          out: handoffkit-artifacts
          comment: "false"
